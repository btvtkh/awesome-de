return {
	button = require("widgets.button"),
	check = require("widgets.check"),
	icon = require("widgets.icon"),
	input = require("widgets.input"),
	label = require("widgets.label"),
	menu = require("widgets.menu"),
	remote = require("widgets.remote"),
	scale = require("widgets.scale"),
	switch = require("widgets.switch")
}
